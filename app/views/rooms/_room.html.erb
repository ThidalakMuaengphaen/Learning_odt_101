<div id="<%= dom_id room %>">
  <p>
    <strong>Name:</strong>
    <%= room.name %>
  </p>
  <%= link_to messages_path(room_id: room.id), 
            data: { turbo_frame: 'room-messages' } do %>
    <div class='room'>
      <%= room.name %>
    </div>
  <% end %>
</div>
